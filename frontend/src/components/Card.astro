---
const { title, class: classList } = Astro.props;
---

<div class:list={["container card bg-light", classList]}>
	<div class="card-body">
		<h6 class="card-title">{title}</h6>
		<slot />
	</div>
</div>
